<%= form_with(model: leaf, url: [@quire, leaf], local: true) do |form| %>
  <% if leaf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leaf.errors.count, "error") %> prohibited this leaf from being saved:</h2>

      <ul>
        <% leaf.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :folio_number %>
    <%= form.text_field :folio_number %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="field">
    <%= form.label :single %>
    <%= form.check_box :single %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
